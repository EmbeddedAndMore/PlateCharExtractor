<UserControl x:Class="PlateLetterExtractor.Controls.ZoomableImageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PlateLetterExtractor.Controls"
             mc:Ignorable="d"
             Name="ZImage"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ScrollViewer Name="MyScrollViewer" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Image Name="MyImage" Source="{Binding SourceImage , ElementName=ZImage}" Stretch="None" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" MouseWheel="MyImage_MouseWheel" MouseLeftButtonDown="MyImage_MouseDown" MouseLeftButtonUp="MyImage_MouseUp" MouseMove="MyImage_OnMouseMove">
                <Image.RenderTransform>
                    <TransformGroup>
                        <TranslateTransform/>
                        <ScaleTransform/>
                    </TransformGroup>
                </Image.RenderTransform>
                <Image.RenderTransformOrigin>
                    <Point X="0.0" Y="0.0"/>
                </Image.RenderTransformOrigin>
            </Image>
        </ScrollViewer>
    </Grid>
</UserControl>
